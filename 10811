namespace Baekjoon
{
    class ReverseBasket
    {
        static void reverseBasket()
        {
            bool a = Console.ReadLine()[0] == 45;
            int b = Console.Read() != 45 ? 0 : 2;
            Console.Write((a != b > 0 ? 2 : 1) + b);
        }
    }
}